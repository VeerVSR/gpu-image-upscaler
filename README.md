# GPU Image Upscaler (Real-ESRGAN)

GPU-based image upscaling using Real-ESRGAN and PyTorch CUDA.

## Requirements
- Python 3.10
- NVIDIA GPU (tested on GTX 1650)
- CUDA 11.8

## Setup

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
